#include<stdio.h>

void main()
{ 
	int age;
    printf("enter your age :");
    scanf("%d", & age);
    if (age >=1 && age<=100)
    {
    printf("valid");
    }
    else
    {
    printf("invalid");
    }
    
    
    
}
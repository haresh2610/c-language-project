#include<stdio.h>

void main()
{
		int s1,s2,s3,s4,s5,s6,total;
    	float per;
        printf("enter 6 subject marks");
        scanf("%d %d %d %d %d %d ",&s1 ,&s2,&s3,&s4,&s5,&s6);
        if(s1<0 || s1>100)
        {
        printf("invalid subject 1 mark...");
        }
        
        { if (s2<0 || s2>100)
        printf("invalid subject 1 Mark..");
        }
              {  if (s3<0 || s3>100)
        printf("invalid subject 1 Mark..");
        }
        
            {    if (s4<0 || s4>100)
        printf("invalid subject 1 Mark..");
        }
           { if (s5<0 || s5>100)
        printf("invalid subject 1 Mark..");
        }
          {  if (s6<0 || s6>100)
        printf("invalid subject 1 Mark..");
        }
    
    
}
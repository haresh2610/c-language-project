#include<stdio.h>

void main()
{
	 int a = 100,b=1, add ,sub,mul,div,mol;
     add =a+b;
     sub=a-b;
     mul=a*b;
     div=a/b;
     mol=a%b;
     
     printf("add : % d", add);
     printf("\n sub :%d",sub);
     printf("\n mul : %d ",mul);
     printf("\n div : %d",div);
     printf("\n mol : %d",mol);
     
}
